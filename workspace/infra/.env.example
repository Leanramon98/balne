# Email — Resend (https://resend.com)
# Sandbox mode: solo entrega a tu email registrado en Resend
RESEND_API_KEY=re_CXuFcQBS_Lq8RWKaEgMAgPwfrFuxQWF6a
# Dejalo asi en sandbox, cambialo cuando tengas dominio propio
RESEND_FROM=onboarding@resend.dev

# Frontend URL (para links en los emails)
FRONTEND_URL=http://localhost:3000

# Logo URL para emails (si no se setea, usa FRONTEND_URL + /logo.webp)
LOGO_URL=https://example.com/logo.webp

# Templates editables (montado como volumen en infra/email-templates/)
# El diseniador modifica los archivos .html ahi y los cambios se ven al instante
EMAIL_TEMPLATES_DIR=/app/email-templates
