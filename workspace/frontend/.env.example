# Enable only while running `npm run dev` locally. The BFF and middleware also
# require NODE_ENV=development, so this shortcut cannot work in production.
NEXT_PUBLIC_BALNE_LOCAL_DEMO_LOGIN=true

# Required only for real authentication through the local gateway.
# INTERNAL_GATEWAY_URL=http://localhost:8080
